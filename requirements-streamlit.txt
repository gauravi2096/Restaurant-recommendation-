# Streamlit app + recommendation engine (Phases 1–3)
# Include FastAPI because phase2_api/__init__.py imports .api (create_app), which uses FastAPI.
# datasets, pandas, huggingface_hub for first-time DB creation when restaurants.db is missing.
streamlit>=1.28.0
pydantic>=2.0.0
fastapi>=0.104.0
groq>=0.4.0
python-dotenv>=1.0.0
datasets>=2.14.0
pandas>=2.0.0
huggingface_hub>=0.17.0
